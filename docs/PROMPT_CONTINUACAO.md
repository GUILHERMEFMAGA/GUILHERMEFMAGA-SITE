# PROMPT DE CONTINUAÇÃO (cole na conversa nova do Agent Mode)

CONTEXTO: você é o Agent Mode e vai CONTINUAR um projeto grande que já existe — NÃO comece do zero.

REPOSITÓRIO: GUILHERMEFMAGA/GUILHERMEFMAGA-SITE. O trabalho da SESSÃO ATUAL está na branch `arena/01a09bca-guilhermefmaga-site` — que é também a branch de ENTREGA do atualizador do agente (constante `RAMO_OFICIAL` em agente.py + 3 URLs no iniciar.bat; r81). O PULL REQUEST #4 permanece aberto na branch antiga `arena/01a08d8e-guilhermefmaga-site`, que FICOU CONGELADA NA r73 (histórico; comentários de release continuam sendo postados no PR #4 via `gh pr comment 4`). Diff acumulado ~+56 mil linhas. NÃO troque de branch: comece com `git fetch origin arena/01a09bca-guilhermefmaga-site` e alinhe seu HEAD a ele (`git reset -q --soft $(git rev-parse FETCH_HEAD)` se o local divergir — o sandbox às vezes volta ao base; NUNCA use `--hard`).

O PROJETO: "Super Agente PC" — agente para Windows, 100% LOCAL (sem cota/offline), usuário LEIGO, tudo em português simples. Ele se atualiza sozinho ("atualizar agora" entrega agente.py + main.py + iniciar.bat + requirements.txt e reaplica ao fechar), se diagnostica, executa ações reais no PC (516 ferramentas @tool; destrutivas SEMPRE com confirmação + backup + desfazer) e responde com FATOS: o que o usuário ensina ("conhecimento ensinar topico: fato") sobrevive ao fechar (cerebro.json, r71), volta como resposta instantânea com FONTE (r70), o revisor corrige invenção do modelo GGUF (r70), pergunta pessoal sem fato ganha aula de ensinar (r72), abertura sauda "BEM-VINDO DE VOLTA" (r73), ritmo/céu dry-run (r74), casa: fila/cron/sandbox/cofre/convidado (r75), cérebro ampliado: exportar/importar/diff/auditoria/visão/manual (r76), automação profissional c/ Agendador do Windows (r78), voz: STT local whisper.cpp (r79 — "baixar stt" SEMPRE com 'sim'), resposta organizada: tabelas/gráficos/ideias bonitos na tela (ASCII) + arquivo (HTML/SVG/.md) c/ construtores @tool + rotas leigo + organizador automático (r80), atualização certa + escudo de entrada (r81 — RAMO_OFICIAL único com teste que trava a branch; `_r81_saida_interna` ignora banner do próprio programa misturado no input).

ONDE PARAMOS (14/09/2026): r81 publicada; 898 testes OK (skipped=3). Selo: `[Motor e avaliacao local 2026-09-11-r81]`. Pendências: (1) PC do dono CONSTA na r73 (log real de 14/09 — selo r73 + "conteudo identico" no atualizar) — o salto r73→r81 é UMA VEZ SÓ via comando PowerShell de copiar/colar (já entregue ao dono em 14/09); depois disso o "atualizar agora" funciona para sempre; cobrar prints da abertura e provas de `tabela`/`grafico`/`ideias` (r80), `baixar stt` + `falar` (r79), `agendar windows` (r78); (2) LEVA DE 19 ITENS da LISTA-IMPOSSIVEL está COMPLETA (r74 31–37; r75 28/29/30/39/40/41; r76 14/15/42/43/44; r78 automação a pedido; r79 38 STT); (3) LISTA-100 segue SÓ PROPOSTA; (4) maior futuro: RAG dos documentos do usuário (#11); (5) diretriz do dono (13/09): padrão "super-agente" — novas ferramentas + aprimorar tudo (NADA é tirado) e, ao fim das próximas releases, lista de ideias do próprio agente (SÓ PROPOSTA).

PRIMEIRO PASSO: rode a suíte e confirme 898 OK (venv evapora a cada turno: `python3 -m venv /tmp/pr3check/.venv && /tmp/pr3check/.venv/bin/pip install -q -r requirements-tests.txt`; depois `/tmp/pr3check/.venv/bin/python -m unittest discover -s tests -q`); leia o TOPO de docs/CONTINUIDADE_AGENT_MODE.md (bloco "ESTADO ATUAL") e docs/RETOMADA.md; `git log --oneline -3` deve mostrar a ponta da branch da sessão. Depois resuma ao usuário onde paramos e pergunte: (a) o passo de PowerShell de 14/09 rodou no PC dele? prints? (b) quais próximos itens ele quer? NÃO implemente nada sem ele escolher.

REGRAS DE OURO (completas no diário): dono LEIGO (comandos prontos pra colar, UM por vez, print de qualquer erro); NUNCA apagar nada (aprimorar sem apagar — r55); teste NUNCA importa agente.py (loader AST em tests/test_roteamento_conversa.py — estender a tupla de prefixos no `_rNN_` novo); a cada release: selo + cascata em TODOS os testes (inclusive os 3 literais presos do fake em tests/test_cerebro_casa_r76.py) + suíte verde ANTES de publicar; publicar = commit + push NA branch da sessão + comentário no PR #4; .bat CRLF puro (verificar em bytes); patch em agente.py sempre via python3 com `assert texto.count(ancora)==1` + py_compile; kill-switch na config para toda feature nova; anti-colisão (r30) antes de nome novo; console Windows cp1252 = SÓ ASCII nos renderers de tela (nada de box-drawing); GRAMÁTICA: editar docs SEMPRE lendo o arquivo inteiro antes de gravar (incidente de 14/09: abrir para escrita antes de ler destruiu o diário — recuperado da branch remota).
