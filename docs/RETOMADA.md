# 🧭 PONTO DE RETOMADA — LEIA PRIMEIRO (atualizado em 2026-09-14)

Este arquivo existe para QUALQUER agente (ou humano) novo se situar em 2 minutos
sem depender de memória de conversa anterior. Depois de ler, leia também
`docs/CONTINUIDADE_AGENT_MODE.md` (diário completo, regras e lições).

## ONDE ESTAMOS
- **Branch de trabalho e ENTREGA (única autorizada):** `arena/01a09bca-guilhermefmaga-site` — a da sessão Arena atual (13/09). **O atualizador do agente (agente.py + iniciar.bat) aponta para ESTA branch** (RAMO_OFICIAL, r81). O **PR #4** continua aberto na branch antiga `arena/01a08d8e-guilhermefmaga-site`, que FICOU CONGELADA NA r73 (é só histórico — nunca atualizar a partir dela; lição r81: em 13/09 a branch trocou e as URLs de entrega ficaram paradas na antiga, e o PC do dono ficou 'atualizando' para a r73 para sempre). A `main` está VELHA — nunca basear trabalho nela.
- **Última release publicada: r103** — "O EXE É PEQUENO DE PROPÓSITO" (evidência real do PC do dono, 16/09: print da pasta _visao
mostrou o `llama-server.exe` com **9 KB** — o build atual do llama.cpp no Windows é LAUNCHER +
DLLs, e o piso de 2 MB do r101 era maior que o exe de verdade, mantendo o loop de
re-download): `exe_min_mb: 0` — o exe só precisa existir (a integridade do conjunto vem do CRC
do zip, r101). No PC do dono o próximo `baixar visao` não re-baixa NADA e declara "Visao local
instalada". (A causa do SERVIDOR morrer ao iniciar segue em investigação via o LOG do r102 +
visao.bat.) Antes: r102 — "O SERVIDOR DA VISAO FALA" (falha REAL do PC do dono, 16/09: visão instalada, `interaja com
Náutica gooners` abriu o modo conversa, mas o llama-server MORRIA ao iniciar e o terminal repeteu
"nao abriu em 90s" mudo 6x — o console do servidor ia para DEVNULL e NINGUEM via a causa): (1) o
console do servidor vai para LOG (_visao/servidor_visao.log); (2) processo MORTO -> mensagem com o
CÓDIGO DE SAIDA + as ultimas linhas do log (a causa real; se o log falar em DLL, dica do
Redistribuivel do Visual C++); (3) processo VIVO no fim da espera (carregando 8 GB em disco lento
nao e falha) -> fica no fundo e o modo conversa pega quando a porta abrir; (4) espera 90s -> 240s.
Provas: 4 testes (morto mostra a causa do log; vivo nao e falha; linhas puras; wiring). Antes: r101 — "PEÇAS COM IDENTIDADE (SHA256) + EXE COM CRC" (falha REAL do PC do dono, boot r100, 16/09: `baixar
visao` re-baixava o PROGRAMA a cada execução e NUNCA passava no check final — o piso de 10 MB da r94
era MAIOR que o exe do build atual; e o projetor cortado pela rede no download paralelo (bug r99)
ficou com o TAMAHO certo mas CONTEUDO corrompido, passando no check de tamanho): (1) `exe_min_mb: 2`
(piso seguro) + o zip do programa passa em `ZipFile.testzip()` (CRC de cada membro) ANTES de extrair;
(2) cada peça .gguf tem sha256 OFICIAL pinado da fonte (HuggingFace, checado via API): nova função
pura _r101_sha256 (blocos de 1 MB) + _r101_peca_confianca — a peça passa com tamanho de saude E
identidade (bate com o sha de uma das fontes); pre-check, check final e _baixar_com_fontes (após
cada download) usam a confiança — arquivo do tamanho certo com FURO no meio agora é DETECTADO e
re-baixado sozinho. Provas: 9 testes (pins oficiais travados contra typo; zip cortado erra; wiring)
+ E2E completo no código real: fluxo normal com shas conferidas + CASO DO DONO (mmproj do tamanho
certo mas corrompido -> detectado e re-baixado; modelo intacto ficou no lugar). Antes: r100 — "DOWNLOAD VERIFICADO BYTE A BYTE" (falha REAL do PC do dono, boot r99, 15/09 à noite: `baixar visao`
terminou SEM erro visível, mas o projetor (1,94 GB) ficou incompleto — a rede cortou a conexão no meio e
o servidor fechou o stream "de boa"; o modelo de 6 GB estava COMPLETO byte a byte — 5.963.057.216, idêntico
ao oficial): 3 buracos de verificação fechados — (1) o download paralelo usava `_ex.map` SEM CONSUMIR (a
exceção da fatia ficava guardada e ninguém via — o download "terminava" com arquivo incompleto e sem erro!);
(2) cada fatia agora confere status 206 + contagem exata de bytes ("fatia cortada: X de Y bytes" — fatia
curta erra na hora); (3) conexão única com contagem de bytes contra o Content-Length declarado
(_r100_baixar_unico: "conexao cortada: X de Y bytes") — arquivo parcial NUNCA é aceito como completo.
Provas: 5 testes (servidores locais simulando fatia curta, Range ignorado e corte no stream) + E2E
completo no código real sem injeção: rede saudável -> "Visao local instalada (r94)" com peças do tamanho
exato; rede cortada -> erro HONESTO "falhou no meio" (nunca sucesso falso). Antes: r99 — "DOWNLOAD RÁPIDO" (pedido do dono, 15/09 à noite: "TEM COMO FAZER IR RAPIDO" — o download da
visão ~8 GB que estava em curso no PC, com 1 conexão em rede lenta, levaria 1~2 h): agora cada
peça do `baixar visao` é baixada em **4 conexões em paralelo** (HTTP Range — cada fatia baixa seu
pedaço e escreve na posição certa do arquivo; servidor sem Range cai no download único de antes,
nada é tirado; TAMANHO final verificado — peça errada é apagada e re-baixada), com avisos de
progresso antes de cada peça ("modelo ~6 GB — 4 conexoes em paralelo (2~4x mais rapido)"). Função
pura _r99_baixar_paralelo (stdlib só: urllib + threads); provas: 4 testes (download paralelo REAL
contra servidor local com Range — sha256 idêntico ao arquivo original; fatias 1/2/8; sem Range;
wiring) + E2E completo no código real ("baixar visao" + "sim" com servidor Range de verdade ->
"Visao local instalada (r94)"). Limite honesto: 2~4x mais rápido no download; a primeira carga do
modelo na RAM continua o que é. Antes: r98 — "A DICA DO COMANDO" (segunda cara da falha REAL de 15/09: depois do boot r97, uma variação do "atualizar agora" escapou de todas as rotas e foi para o modelo, que respondeu conversa em vez de acao — sem nenhum aviso de que era um comando): agora, quando uma frase CURTA (≤6 palavras) que passou por todas as rotas contem palavra-chave de comando local (atualiz/baixar+stt/mic/visao/visao/diagnostico+microfone/para+conversa/interaja+com...), o agente imprime uma **dica local na hora** com o comando canônico exato e volta ao prompt SEM enviar ao modelo (a pessoa queria uma acao); frase LONGA (pergunta de verdade que só meniona a palavra) -> dica + o modelo também responde (aprimorar sem apagar). Função pura _r98_dica_comando (testável). E2E no código real: "atualizar agora" segue na rota do updater (regressão) + "baixar visao" segue na rota de download (regressão r97) + dica curta consome/longa não consome/comum não gatilha. Antes: r97 — "BAIXAR VISAO ENTENDE O PORTUGUÊS" (falha REAL no PC do dono, 15/09: digitado "baixar visao", o comando NÃO caiu na rota de download e foi para o modelo — que respondeu "não posso ajudar"; a causa: a rota casava pelo texto CRU (`cmd.startswith("baixar visao")`) e não pela forma normalizada — "baixar visao" (com til, a forma natural), "baixar a visao" (com artigo) e variações de caixa/espaco escapavam; o arquivo do PC foi confirmado idêntico ao publicado (2.042.623 caracteres), então a causa era o casamento, não a versão): agora a rota casa pela forma NORMALIZADA (mesma regra da casa das rotas r84/r89/r90 — "baixar", "visao" em qualquer ordem de acento/caixa/espaco/artigo). Provas: E2E no código real — 5 variantes ("baixar visao", "baixar visao", "baixar a visao", "BAIXAR VISAO", espacos duplos) todas chegam na rota sem tocar no modelo + teste de wiring. Antes: r96 — "OLHOS LOCAIS: FONTES PÚBLICAS" (falha REAL no PC do dono, 15/09: `baixar visao` falhou com **HTTP 401** — diagnóstico: o repositório `bartowski/Llama-3.2-11B-Vision-Instruct-GGUF` usado na r94 é **GATED** no HuggingFace (modelos Llama exigem conta + licença; anônimo recebe 401 "Invalid username or password"): agora o download usa **fontes PÚBLICAS** do mesmo modelo Llama 3.2 Vision 11B Q4_K_M — primária `leafspark/...` (arquivos públicos, 8.2K downloads) + reserva `pbatra/...` (se a primária falhar, tenta a outra; peça parcial de fonte morta é apagada) — e o total CAIU de ~15 GB para **~8 GB** (modelo 5.96 GB + projetor 1.94 GB). Limpeza: `.gguf` antigo/incompleto de nome diferente (sobrevivente de download anterior) é apagado e o MB liberado aparece na mensagem. Antes: r95 — "OLHOS LOCAIS: DOWNLOAD COM CONFIANÇA" (correção de bug REAL do download da r94 — no PC do dono o `baixar visao` falhou no meio com HTTPError e a mensagem prometia "continua de onde parou", mas o código só cheava se o arquivo EXISTIA: um .gguf cortado em 4 GB passaria como "instalado" e o servidor ia tentar carregar um modelo quebrado): agora cada peça é conferida por **TAMANHO** (função pura _r94_peca_completa: exe ≥10 MB, modelo ≥7.4 GB, projetor ≥5.8 GB) — peça parcial é apagada e re-baixada (as completas ficam salvas), o download só declara sucesso se as três tiverem tamanho de verdade, e o erro agora mostra a CAUSA (ex.: HTTPError HTTP 403). Antes: r94 — "OLHOS LOCAIS" (opção "A" do dono 14/09 — a visão 100% offline agora o agente BAIXA E LIGA SOZINHO): comando `baixar visao` (padrão do `baixar stt`: confirmação "sim" com aviso de ~15 GB, download da stdlib, injetável) — (1) build do llama.cpp vem da API do GitHub (função pura _r94_achar_zip_windows escolhe o build CPU x64 do Windows mais novo — 2026: "releases/latest" é placeholder, os builds são releases bNNNN); (2) modelo Llama 3.2 Vision 11B (bartowski GGUF, ~8 GB) + (3) projetor mmproj-F16 (~6 GB) vão para _visao/modelos_visao (NADA sai do PC); LIGA SOZINHO: no "interaja com NOME" e no ciclo do modo conversa, se as peças estão no lugar e a porta está fora, o agente abre o llama-server (Popen, registrado p/ o atexit derrubar ao fechar) e espera a porta abrir (1-2 min, freio de 120s contra insistência em falha); visao.bat (CRLF puro) como alternativa manual. + correção de falso positivo REAL: o projetor (mmproj-F16.gguf) é .gguf >20MB — o r76 achava "modelo encontrado" quando SÓ havia o projetor; _r76_visao_achar_modelo ignora mmproj e _r92_leitor_pronto trata "só mmproj" como sem leitor (kill-switch "visao_local" agora respeitado no diagnóstico). Antes: r93 — "O ATUALIZADOR DETECTA PROCESSO ANTIGO" (log real do PC 14/09: o dono rodou 'atualizar agora' e veio 'Voce ja esta na versao oficial mais nova (conteudo identico). Nada foi trocado.' — o ARQUIVO já era novo, mas o PROCESSO rodando era antigo (atualizado antes, sem reiniciar de fato) e o modo conversa continuava com o comportamento velho: 'leitor indisponivel' em loop puxando o WhatsApp; o updater só comparava arquivo x remoto e NUNCA percebia o processo velho): agora o atualizador compara o selo do processo em execução (global _SELO_EM_EXECUCAO, guardado no boot) com o selo do arquivo novo — arquivo em dia + processo antigo -> mensagem honesta + REINÍCIO para aplicar (função já injetável; teste com processo r91 x arquivo r92). Antes: r92 "CEGO NÃO MEXE NA TELA" (log real do PC 14/09, boot r91: 'interaja com Náutica gooners' LIGOU o modo conversa, mas sem leitor — sem visão local nem GEMINI_API_KEY — o terminal ficou repetindo 'leitor indisponivel' e o WhatsApp PULAVA A FRENTE a cada 30s enquanto o dono assistia vídeo): (1) agora o ciclo confere o leitor (função pura _r92_diagnosticar_leitor + _r92_leitor_pronto: modelo r76 + servidor na porta OU GEMINI_API_KEY) ANTES de focar a janela — cego, ele NABRE o WhatsApp e aguarda quieto; quando o leitor é ligado, conecta sozinho no ciclo seguinte; (2) no INÍCIO do modo, diagnóstico honesto no terminal explicando exatamente o que falta (servidor da visão local na porta / GEMINI_API_KEY) e os dois caminhos para enxergar (visão local 100% offline ou nuvem). Antes: r91 "SEM MENSAGEM PERDIDA + INTERAJA À PROVA DE VARIANTES" (2 falhas reais do log do PC 14/09, boot r90): (1) 'mande oi para +55 51 9268-6262' — o envio para NÚMERO usava a busca por NOME do app; número cru não aparece nessa busca, o Enter não abriu conversa e a mensagem foi colada NA CAIXA DE BUSCA (o dono viu literalmente '+55 51 9268-6262oi') — agora número cru abre a conversa DIRETA por deep link (whatsapp://send?phone=<digitos>, função pura _r91_preparar_envio_numero) e avisa se o app mostrar a tela de CONVIDAR (número fora do WhatsApp); (2) 'interaja com Náutica gooners' ainda caía no modelo no PC — a forma normalizada (sem espaços/acentos, à prova de espaço duplo/espaco especial/acento/maiúscula/typo) agora também liga o MODO CONVERSA, como a rota de parar já fazia. Antes: r90 "O AGENTE ENTENDE 'INTERAJA COM' E 'MANDE MENSAGEM PARA NÚMERO'" (2 falhas reais do log do PC 14/09): (1) 'intareja com Náutica gooner' (typo real do dono, digitado no boot r89) — a forma natural 'interaja com NOME' (e 'interage/interagir com', 'intareja') agora ABRE o MODO CONVERSA (antes caía no modelo, que respondeu 'não posso interagir'); (2) 'mande oi para +55 51 9268-6262' (mensagem ANTES do 'para' com número cru — o parse exigia a palavra 'mensagem') — agora manda direto para o número (o número torna o alvo inequívoco; nomes sem 'mensagem' continuam na trava, por ambiguidade). Provas: 8 casos de parse (função pura) + E2E no código real com as frases exatas do log (typo abre o modo; envio (número, 'oi'); cauda após vírgula; nome não envia). Antes: r89 "PARA ENTENDE O PORTUGUÊS" (pergunta do dono 14/09: "e no caso para parar de interagir?" — a parada do MODO CONVERSA agora aceita TODAS as formas naturais: 'para de conversa', 'para conversa', 'para modo conversa', 'para o modo conversa', 'sair do modo conversa', 'desliga modo conversa', 'desligar o modo conversa', 'para whatsapp', 'para de interagir', 'para interagir', 'para de responder', 'para responder', 'para a interacao', 'parar interacao', 'para de interagir na conversa' — 17 gatilhos normalizados na rota; PROVA: 16/16 formas cobrem a rota (100% local, sem modelo) + E2E no código real: 'para de interagir' com o modo ATIVO encerra a thread de fundo; +1 teste (test_formas_naturais_de_parar). Antes: r88 "MODO CONVERSA WHATSAPP" (pedido do dono 14/09: "ele consegue falar conversando no WhatsApp infinitamente, lendo a conversa e respondendo, mesmo eu fazendo outra coisa no PC, com humor humano"): o agente conversa SOZINHO no WhatsApp em THREAD DE FUNDO — a cada intervalo foca o app, lê a tela por VISÃO (1º a visão local r76 100% offline, depois Gemini na nuvem), decide (função pura c/ anti-eco — nunca responde duas vezes a mesma mensagem) e compõe a resposta pela IA LOCAL primeiro (privado/offline/custo zero) ou nuvem, com persona de gente de verdade no WhatsApp (curto, direto, humor leve, sem cara de robot); comandos: 'modo conversa NOME' (pergunta confirmação), 'status do modo conversa', 'para de conversa' (para na hora); kill-switch 'modo_conversa_wpp' no config.json; HONESTIDADE: a janela do WhatsApp vem a frente por alguns segundos a cada verificação, e o leitor exige visão local (r76) ou GEMINI_API_KEY. + correção do log real: 'quantos contatos eu tenho' caía no modelo (que respondeu "não tenho acesso" — falso) — agora vai na rota local. Funções puras testáveis: _r88_extrair_ultima_externa / _r88_decidir / _r88_prompt_resposta / _r88_dormir / _r88_loop_conversa (23 testes novos). Antes: r87 "WHATSAPP COM MEMÓRIA E AUDITORIA" (aprimoramento no nível alto, a pedido do dono 14/09: "sem tirar nada, só pôr suas ideias em código — mas tem que ser extremamente boas"; 4 ideias nascidas do que o código JÁ fazia e deixava no meio): (1) "minhas mensagens" — o agente JÁ registrou cada envio em logs_whatsapp.json mas NUNCA tinha como ver: agora mostra os últimos envios (data -> destino: mensagem); (2) "qual o numero do X" / "quem é +55..." — procura na agenda por nome (exato ou parecido) ou por número (completo ou só a parte final: "quem é 991328338"); (3) o log de envio agora guarda o DESTINO (o envio por busca no app registrava numero="" e o histórico não mostrava pra quem foi); (4) "desfazer contato" — salvar/apagar contato agora guarda um snapshot (contatos.json.ultimo) antes de mudar e o comando restaura o último estado (o "desfazer" genérico já era usado pela edição de projetos). BÔNUS: após mandar para um NÚMERO CRU que não está na agenda, o agente pergunta UMA vez o nome para salvar. Funções puras testáveis: _r87_ultimos_envios_wpp / _r87_procurar_contato (24 testes novos). Antes: r86 "WHATSAPP DE VERDADE" (aprimoramento no nível alto + correções do log real 14/09, a pedido do dono): (1) mandar DIRETO PARA NÚMERO — "mande mensagem pra +55 16 99132-8338, se vai jogar?" — via busca no app (antes não existia rota para número cru: caía na trava genérica); (2) a VIRGULA agora separa nome e mensagem — "manda whatsapp pro CORVOS DE CARTOLA OFC, ola tudo bem" (antes o nome virava a frase inteira e o agente respondia "faltou a mensagem"); (3) "salve esse contato +55 16 99132-8338" — as formas com "salve" agora são gatilho de salvar contato, e número SEM nome PERGUNTA o nome uma vez e salva (antes caía no modelo, que respondia "não posso salvar contatos"); (4) o erro de digita "menssagem" é reconhecido; função pura `_r86_parse_whatsapp` (14 testes novos). Antes: r85 "O AGENTE FALA O QUE FEZ" (3 correções do log real 14/09: (1) a rota do diagnóstico cobre o verbo conjugado — 'diagnostica o microfone' — via caule 'diagnost'; (2) o 'atualizar agora' IMPRIME a troca ANTES de reiniciar — antes a troca acontecia em silêncio (os._exit antes do print) e o dono via só os 3 [OK] do lançador e achava que nada foi trocado, embora o boot seguinte já estivesse na versão nova; (3) pedido de mandar mensagem que não casa com o comando recebe a orientação certa: 'manda whatsapp pro NOME: a mensagem' (rota 100% local) em vez do genérico 'adicione essa ação'). Antes: r84 "DIAGNÓSTICO ENTENDE O PORTUGUÊS" (bug real do log 14/09: na r83 a rota só casava 'diagnostico microfone' exato; a forma natural 'diagnostico de/do microfone' escapou para o modelo, que respondeu 'não tenho acesso ao microfone' — condição agora robusta por palavras, sem ordem fixa nem preposições). Antes: r83 "DIAGNÓSTICO DE MICROFONE" (bug real do log 14/09: `falar` falhou com `PortAudioError` opaco no PC do dono — agora existe `diagnostico microfone`, que lista quais microfones o Windows expõe e diz o que fazer em PT-BR; `stt` mostra a linha do microfone; o erro do `falar` imprime a causa real). Antes: r82 "CAIXA DO DONO" (correção cosmética pedida pelo log real: as rotas `tabela`/`grafico`/`ideias` extraíam os dados do comando MINUSCULO e o que o dono digitou com maiúsculas saía em minúsculas — agora os dados vêm do texto ORIGINAL; condição da rota segue minúscula como antes). Antes: r81 "ATUALIZAÇÃO CERTA + ESCUDO DE ENTRADA" (correção do bug real do PC do dono: as URLs de atualização apontavam para a branch ANTIGA congelada na r73 — agora RAMO_OFICIAL único em agente.py + iniciar.bat, com teste que trava; + escudo determinístico que impede o texto do PRÓPRIO programa — banners, prévias de geração — de virar "comando" e ser respondido pelo modelo). Antes: r80 "RESPOSTA ORGANIZADA" (a IA LOCAL e o dono constroem tabelas, gráficos e listas de ideias BONITOS: na tela em ASCII + arquivo autocontido p/ navegador — HTML c/ CSS/SVG embutido e .md; + organizador automático; kill-switch `organizar_respostas`), r79 "VOZ" (STT local whisper.cpp — o item 38 da leva de 19, a "obra grande"; era a reserva r77, publicada como r79 porque o selo segue a ordem de publicação: `falar`/`ouvir`, `stt`, `baixar stt` c/ 'sim'; áudio NUNCA sai do PC; kill-switch `stt_local`), r78 "AUTOMAÇÃO PROFISSIONAL" (Agendador do Windows), r76 "CONHECIMENTO E CASA AMPLIADA", r75 "A CASA", r74 "RITMO E CÉU" (dry-run), r73 "BOAS-VINDAS DE VOLTA" (`sessao.json`), r72 honestidade, r71 cérebro persistente.
- **Suíte: 1050 testes OK** (skipped=3). `agente.py`: ~41,8 mil linhas, 516 ferramentas @tool, 24+6+7+6+1+5 rotas de comando. Selo atual: `[Motor e avaliacao local 2026-09-11-r103]` (o número r77 não foi usado — o STT saiu como r79, na ordem de publicação).
- **Auditoria completa 2026-09-13: TUDO CERTO** (0 nomes indefinidos, 0 rotas mortas, 0 duplicatas; laudo em `esqueletos_ideias/2026-09-13-auditoria-completa.md`).

## HISTÓRIA RECENTE (resumo — o resto está no diário)
- r64–r65: autodiagnóstico na abertura + guarda anti-invenção. r66: gatilhos de atualização. r67: lote de 20 (velocímetro, checkpoints, modo avião, config, `sair e atualizar`...). r68: lote de 30 (cérebro indexa, duplicatas, rpg, organizador, geladeira...). r69: NÚCLEO ABSURDO (pesos/erosão/conhecimento/EMA/fases no motor). r70: FORA DO PADRÃO (fatso por overlap, revisor que corrige o modelo, modo detalhado, dica de próximo passo). **r71: CÉREBRO PERSISTENTE** (`cerebro.json` — o que o usuário ensina SOBREVIVE ao fechar; `esquecer <frase>`; `listar aprendizado`; requirements.txt pinado entregue pelo `atualizar agora`). **r72: HONESTIDADE** (pergunta pessoal sem fato = aula de ensinar, nunca papo furado). **r73: BOAS-VINDAS DE VOLTA**. **r74: RITMO E CÉU** (`replay erros`, `testar malha`, `guardiao gatilhos`, `traduzir rota <frase>`, `criar rota <gatilho>: <ação>`, `aquecer as HH:MM` — tudo dry-run). **r75: A CASA** (`fila: <tarefa>`, `cron adicionar <nome> as HH:MM: <tarefa>` c/ dias da semana, `diario lentidao`, `sandbox: <código>` com 'sim', `cofre chaves` c/ DPAPI, `modo convidado`/`sair do modo convidado`). **r76: CONHECIMENTO E CASA AMPLIADA** (`exportar cerebro`, `importar cerebro: <arquivo>` c/ mescla, `diff cerebro` "o que aprendi esta semana?", `auditoria completa` relatório único, `ver <imagem>` visão c/ modelo SEPARADO, `manual` gerado do próprio código). **r78: AUTOMAÇÃO PROFISSIONAL** (`agendar windows <nome> as HH:MM: <comando>` no Agendador do Windows — roda com o agente fechado; + correção real do parse da rota cron da r75). **r79: VOZ** (`falar`/`ouvir` com STT local whisper.cpp; `stt` status; `baixar stt` com 'sim' explícito; audio nunca sai do PC; LEVA DE 19 ITENS COMPLETA: r74 = 31–37 ✓; r75 = 28,29,30,39,40,41 ✓; r76 = 14,15,42,43,44 ✓; r79 = 38 STT ✓). **r80: RESPOSTA ORGANIZADA** (`construir_tabela`/`construir_grafico`/`construir_ideias` como ferramentas @tool + rotas `tabela`/`grafico`/`ideias` do leigo; tela ASCII + arquivo bonito autocontido — HTML c/ SVG, .md; organizador automático alinha listas/tabelas da IA). **r81: ATUALIZAÇÃO CERTA + ESCUDO** (RAMO_OFICIAL único — agente.py E iniciar.bat entregam da branch da sessão; teste trava a branch; `_r81_saida_interna` ignora banner do próprio programa misturado no input). **r82: CAIXA DO DONO** (rotas `tabela`/`grafico`/`ideias` preservam a caixa do que o dono digitou — bug real: 'CPU, RAM' saía 'cpu, ram').
- r70/r71 PROVADAS e2e NO PC REAL do dono (fato ensinado respondido na hora com fonte; revisor flagrou mentira do GGUF; cascata r64→r71 num único `atualizar agora`).

## ONDE O DONO ESTÁ / PRÓXIMOS PASSOS
1. **O PC do dono está na r102/r103** (PROVA REAL 16/09: visão instalada com shas conferidas; o llama-server MORRE ao iniciar — causa ainda desconhecida; print do dono da pasta _visao: 51 arquivos, build LAUNCHER+DLLs, llama-server.exe = 9 KB — o piso de 2 MB do r101 era maior que o exe de verdade: r103 = exe_min_mb 0). Suspeita principal p/ a morte do servidor: Redistribuivel do Visual C++ ausente (ou RAM insuficiente p/ os 8 GB). Próxima prova: (1) `visao.bat` na pasta AGENTE_PC (um nivel acima do _visao!) -> print do console; (2) `atualizar agora` (-> r103) + `baixar visao` (nao re-baixa nada, declara instalada) + `interaja com Náutica gooners` (a causa aparece no terminal via o LOG do r102) (PROVA REAL 14/09: boot com selo -r85 após `atualizar agora` — salto r83 → r85 OK; a mensagem "Baixei a versao nova" não apareceu na tela porque o código que RODAVA durante a troca era o r83 — silencioso, o fix r85 ainda não existia lá; e `diagnostico microfone` na r83 respondeu 'não tenho acesso' porque a forma digitada levou o comando ao modelo — a partir da r84/r85 a rota cobre qualquer forma natural, inclusive o verbo 'diagnostica'). O PC tinha feito antes: `tabela` provado no PC real com caixa preservada (r82) + arquivo `.html` novo a cada execução; escudo r81 provado: zero papos com banner). **OS TRÊS CONSTRUÍDORES DA r80 ESTÃO PROVADOS NO PC REAL** (14/09): `tabela` (caixa preservada + `.html`), `grafico` (barras `#` na tela + SVG em `graficos/*.html`), `ideias` (lista numerada + `.md` em `ideias/`) — o pedido original 'tudo bonitinho e organizado em 100%' está CUMPRIDO na prática. **PROVA STT em andamento (14/09)**: `baixar stt` OK no PC real (programa + modelo em `_stt/`); `falar` falhou com `PortAudioError` (microfone — provável falta de microfone PADRÃO no Windows) → r83 criou o `diagnostico microfone` para ver exatamente o que falta. **Próximas provas de fogo** (uma por vez): (a) `atualizar agora` (p/ r98 — deve imprimir "Baixei a versao nova" ANTES de reiniciar — o fix r85 age pela primeira vez; se o processo estiver antigo, o r93 avisa e reinicia); (b) `quantos contatos eu tenho` (agora responde local); (c) WhatsApp nível alto: `mande mensagem pra +55 16 99132-8338, oi` (direto para NÚMERO, via busca no app — e ele PERGUNTA o nome para salvar) + `manda whatsapp pro Fulano, mensagem` (vírgula) + `salve esse contato +55 ...`; (d) memória/auditoria r87: `minhas mensagens` + `qual o numero do Joao Iser` + `quem é +55 16 99132-8338` + `desfazer contato`; (e) MODO CONVERSA r88: `modo conversa NOME` (precisa de leitor: visão local r76 ou GEMINI_API_KEY — ele explica se faltar) + `status do modo conversa` + `para de conversa`; (f) `diagnostica o microfone` ou qualquer forma + print; (c) corrigir o microfone no Windows conforme o diagnóstico + `falar` de novo (deve transcrever); (d) `agendar windows` (r78 — prova real de schtasks); (e) conversa livre pedindo tabela/gráfico pra IA local montar sozinha (ferramentas @tool da r80).
2. **Leva autorizada pelo dono: 19 itens da LISTA-IMPOSSIVEL** (`esqueletos_ideias/2026-09-13-lista-impossivel-ia-local.md`) — **LEVA DE 19 ITENS COMPLETA** (r74 = 31–37; r75 = 28,29,30,39,40,41; r76 = 14,15,42,43,44; r78 = automação profissional a pedido do dono; r79 = 38 STT local). O 'sim' explícito do dono para o download do whisper.cpp foi dado em 13/09; no PC dele o `baixar stt` confirma de novo (regra da casa).
3. LISTA-100 (50 funções + 50 ferramentas do dia a dia, SÓ PROPOSTA) segue à disposição do dono.
4. Diretriz do dono (13/09, após r74): elevar o NÍVEL DE PADRÃO para "super-agente" — novas ferramentas/funções + aprimorar tudo do agente e da IA local (NADA é tirado); ao fim das próximas releases, entregar também uma lista de ideias do próprio agente (SÓ PROPOSTA).
5. **Obra grande combinada (opção B): RAG dos documentos do dono** — agente indexa o CONTEÚDO dos arquivos dele e responde por eles (100% local). Só começar quando o dono pedir.

## REGRAS DE OURO (valem SEMPRE — detalhes no diário)
- Dono é LEIGO: português claro, comandos PRONTOS pra colar, um por vez; pedir print de qualquer erro.
- NUNCA apagar nada (só aprimorar); nunca memória de longo prazo oculta (r33); esqueletos em `esqueletos_ideias/` (r29); listas de ideias = SÓ PROPOSTA com anti-colisão (r30) antes de implementar.
- IA 100% LOCAL (nuvem só se o dono digitar `ligar ia`); GGUF só troca com "sim" explícito; responder com FATOS, nunca com conselho técnico inventado.
- Toda funcionalidade nova tem kill-switch na config (`_r67_ler_config`).
- Testes NUNCA importam agente.py — usam o loader de `tests/test_roteamento_conversa.py` (estender tupla até `'_rN_'` novo). A cada release: bump do selo `2026-09-11-rN` + cascata do selo em TODOS os testes.
- Publicar: commit + push NESTA branch + comentário no PR #4 (padrão `/tmp/msg.txt` + `gh pr comment 4 --repo GUILHERMEFMAGA/GUILHERMEFMAGA-SITE --body-file ...`). Corpo/docs do diário: SEMPRE editar com cwd no repo.
- Cuidados de sandbox: venv evapora (recriar: `python3 -m venv /tmp/pr3check/.venv && /tmp/pr3check/.venv/bin/pip install -q -r requirements-tests.txt`); remoto pode andar sozinho (cura: `git fetch origin arena/01a09bca-guilhermefmaga-site` + `git reset --soft FETCH_HEAD` se HEAD divergir); REM com parêntese dentro de bloco `if (...)` no .bat fecha o bloco.

## COMO PROVAR QUE VOCÊ ESTÁ NO LUGAR CERTO
```
python3 -m venv /tmp/pr3check/.venv && /tmp/pr3check/.venv/bin/pip install -q -r requirements-tests.txt
cd /home/user/GUILHERMEFMAGA-SITE  (ou onde estiver o clone)
/tmp/pr3check/.venv/bin/python -m unittest discover -s tests -q
```
Esperado: `Ran 1050 tests ... OK (skipped=3)` e o selo `-r103` em agente.py. Deu certo? Você está EXATAMENTE onde paramos.

## MAPA DOS ARQUIVOS QUE IMPORTAM
- `agente.py` — o cérebro todo (monólito de propósito; não reformar sem pedido).
- `main.py`, `iniciar.bat`, `requirements.txt` — lançador/BAT/deps (entregues pelo `atualizar agora`, r62).
- `docs/CONTINUIDADE_AGENT_MODE.md` — diário completo r1→r103 + regras + lições + posicionamento honesto.
- `esqueletos_ideias/` — listas (20/30/50+40/80/100/impossível = SÓ PROPOSTA), análise de lacunas, auditoria.
- `tests/` — 93 arquivos, 1050 testes (loader em test_roteamento_conversa.py).
